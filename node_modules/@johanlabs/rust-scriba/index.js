const Database = require("./better-sqlite3.js");
const RS = require("./rust-scriba.node");

const { runBenchmark } = require("./benchmark");

module.exports = {
    default: RS,
    runBenchmark,
    Database
};
